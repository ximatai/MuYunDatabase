package net.ximatai.muyun.database.core.orm;

/** Database-neutral aggregate functions supported by a single-table aggregate query. */
public enum AggregateOperation {
    COUNT, SUM, AVG, MIN, MAX
}
